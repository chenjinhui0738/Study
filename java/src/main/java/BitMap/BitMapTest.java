package BitMap;

import org.junit.jupiter.api.Test;

/**
 * Bit-map的基本思想就是用一个bit位来标记某个元素对应的Value，而Key即是该元素。由于采用了Bit为单位来存储数据，因此在存储空间方面，可以大大节省
 * BitMap用于大量数据的快速排序、查找、去重。布隆过滤器就是bitmap的实现
 */
public class BitMapTest {
    @Test
    public void Test1(){
        //BloomFilter.create()
    }
}
